import React from 'react';
import FormComponent from './FormComponent';

const Form = () => {
    return (
        <section className="form">
            <FormComponent/>
        </section >
    );
};

export default Form;